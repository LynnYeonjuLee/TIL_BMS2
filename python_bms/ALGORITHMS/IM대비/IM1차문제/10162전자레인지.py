# A : 5*60 , B : 1*60 , C : 10 
T = int(input())
# 입력받은 T초가 300으로 나누어서 1보다 크면 

if T % 10 != 0 :
    print(-1)
else:
    A = T // 300
    B = (T % 300) // 60
    C = ((T % 300) % 60 )// 10
    print(A, B, C)
