N = input(int(())
num_list = list(map(int,input().split()))
cnt = 1
length = 1
for i in range(1, N):
    for j in range(len(num_list)):
        num_list[0] = NUM
        if num_list[j] >= NUM;
            num_list[j] = NUM
            cnt += 1
        else:
            cnt = 1
        # if length < cnt:
        #     length = cnt

        
            